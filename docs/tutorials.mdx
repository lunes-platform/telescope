---
sidebar_position: 1
description: Lunes Tutorial about lunes node, lunes wallet, about everything!
keywords: [how to install lunes node, how lunes node work, lunes node tutorial, how to miner in lunes network, 
lunes network, lunes blockchain, lunes plataform miner, how to make a POS in lunes plataform]
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# 🥷 Tutorials

## How to Lunes Node works

The Lunes Blockchain is a decentralized and distributed network based on the LPoS consensus algorithm. This network can be mainnet or testnet, in mainnet the nodes work with real values of tokens, currencies, NFT and assets. On the other hand, testnet has "fake" tokens to provide a test environment.
In the dev language, mainnet is prod and testnet is dev.

Lunes Node is a node or server that has the responsibility to maintain and validate the blockchain, broadcast new transactions and provide an api for interaction and integration with the network.

With a Lunes Node you become a Lunes Blockchain participant and can provide network resiliency, validate data, and integrate real-world services into the blockchain.

## How to Install

<Tabs>
  <TabItem value="🐧 Linux" label="🐧 Linux" default>

  Your need install **[java8](http://www.google.com/search?q=how+to+install+java+8+linux)**

  Get your version of **[Lunes Node](./releases)**

  <details>
    <summary>Lunes Node config file</summary>

```yml title="lunesnode.conf"
# Minimum needed to run
lunes {
    # where will the blockchain be persisted
    directory = "./"

    # choice your chain MAINNET or TESTNET
    blockchain.type = ""

    # put ip:port of any node in chain that you choiced
    # MAINNET 5.196.155.34:7770
    # TESTNET 5.196.155.46:7770
    network.known-peers = [""]

    # It will hide (encrypt) your seed
    wallet.password = ""

    # Your seed hashed in BASED58
    wallet.seed = ""
  }
  ```

- Complete config file **[here](https://github.com/lunes-platform/lunesnode/blob/master/src/main/resources/application.conf)**

</details>


  **Files tree**

  ```js title="/path/mylunesblockchain/"
    ├── lunesnode-latest.jar
    └── lunesnode.conf
  ```

  **For running**

  ```bash
  java -jar lunesnode-latest.jar lunesnode.conf
  ```

  **If you want enable Lunes Node to run forever, create a service**

  <details>
    <summary>Systemd config file</summary>

  ```systemd title="/etc/systemd/system/lunesnode.service"
  [Unit]
  Description = Lunes Node Blockchain
  After = network.target

  [Service]
  WorkingDirectory = /path/mylunesblockchain/
  ExecStart = /usr/bin/java -jar lunesnode-latest.jar lunesnode.conf
  Restart = always
  RestartSec = 30s
  StandardOutput = journal
  StandardError = journal
  SyslogIdentifier = lunesnode

  [Install]
  WantedBy = multi-user.target
  ```

  </details>

  </TabItem>

<TabItem value="🐳 Docker" label="🐳 Docker">

  ```text
  Comming Soon
  ```

</TabItem>

<TabItem value="🍎 MacOs" label="🍎 MacOs">

  ```text
  Comming Soon
  ```

</TabItem>

<TabItem value="🪟 Windows" label="🪟 Windows">

  You have to install **[java8](https://www.java.com/pt-BR/download/)**

- After install, verify your version of java in **CMD** line
  
  ```bash
  java -version
  ```

  Get a version of **[Lunes Node](./releases)**

  Inside your folder make a file with a name **lunesnode.conf** with this content: 

  <details>
    <summary>Lunes Node config file</summary>

  ```yml title="lunesnode.conf"
  # Minimum needed to run
  lunes {
    # where will the blockchain be persisted
    directory = "./"

    # choice your chain MAINNET or TESTNET
    blockchain.type = ""

    # put ip:port of any node in chain that you choiced
    # MAINNET 5.196.155.34:7770
    # TESTNET 5.196.155.46:7770
    network.known-peers = [""]

    # It will hide (encrypt) your seed
    wallet.password = ""

    # Your seed hashed in BASED58
    wallet.seed = ""
  }
  ```

  - Complete config file **[here](https://github.com/lunes-platform/lunesnode/blob/master/src/main/resources/application.conf)**

  </details>

  **Files tree**

  PATH = the path where you saved the file 

  ```js title="/PATH/mylunesblockchain/"
    ├── lunesnode-latest.jar
    └── lunesnode.conf
  ```

  - After getting a version o Lunes Node, in **CMD** line, get inside in your folder when you save this files:
 
  ```bash
  cd /PATH/mylunesblockchain
  ```
  
  **For running**

  ```bash
  java -jar lunesnode-latest.jar lunesnode.conf
  ```

  **If you want enable Lunes Node to run forever, create a service:**

  In the line of **CMD** type it:
  
  ```cmd
  sc create LunesNode binPath= "java -jar PATH\lunesnode-latest.jar PATH\lunesnode.conf" start= auto DisplayName= LunesNode
  ```

  To verify a log of the Node, open the CMD with admin and Entry in your folder:

  ```cmd
  cd /PATH/mylunesblockchain
  ```

  and type it:

  ```cmd
  type lunes.log
  ```

</TabItem>

</Tabs>
